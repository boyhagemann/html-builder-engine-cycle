
export default function event(config, children, state) {

    console.log(parents)

    // Extract variables from the config object
    const { properties } = config
    const { text } = properties

}